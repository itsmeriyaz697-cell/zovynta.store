# Required for admin access
ADMIN_PASSWORD=replace-with-a-long-random-password

# Required for durable Vercel order storage; create a Vercel KV/Redis REST database
KV_REST_API_URL=https://your-kv-endpoint
KV_REST_API_TOKEN=your-kv-token

# Optional: use Resend instead of the FormSubmit fallback for owner email delivery
OWNER_EMAIL=itsmeriyaz697@gmail.com
RESEND_API_KEY=re_xxxxxxxxx
FROM_EMAIL=Zovyenta Orders <orders@your-verified-domain.com>

